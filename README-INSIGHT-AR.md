# Insight Amygdala AR
Fan-made cosmic-horror AR mini-game for Android. Camera permission is requested natively, then the app serves its bundled HTML on 127.0.0.1 inside the phone so Chromium treats the page as a secure local origin for getUserMedia().
